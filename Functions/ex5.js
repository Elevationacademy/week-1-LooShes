const checkExists = function(arr, num) {
    for(number of arr) {
        if(number == num) {
            return true
        }
    }
    return false
}

const intArray = [5,9,3,8,1]
const num = 2

console.log(checkExists(intArray, num))